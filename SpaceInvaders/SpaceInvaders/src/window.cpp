#include "window.h"
#include "player.h"


void window::createWindow(sf::RenderWindow &gWindow) {


	gWindow.setPosition(sf::Vector2i(100, 100)); // setting the pos of the window

	gWindow.setFramerateLimit(60); // setting the framerate

	
}
